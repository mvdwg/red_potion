<h2>Edit Counter value</h2>

<%= render "form.html", changeset: @changeset,
                        action: counter_value_path(@conn, :update, 1, @counter_value) %>

<span><%= link "Back", to: counter_value_path(@conn, :index, 1) %></span>
